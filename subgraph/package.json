{
  "name": "meta-moguls",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ meta-moguls",
    "create-local": "graph create --node http://localhost:8020/ meta-moguls",
    "remove-local": "graph remove --node http://localhost:8020/ meta-moguls",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 meta-moguls"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
